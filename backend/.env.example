# MongoDB
MONGODB_URL=mongodb://localhost:27017
MONGODB_DB_NAME=meta-vis-dev

# Static files — base directory under which Krona HTML files are served
STATIC_FILES_ROOT=/data/taxprofiler

# App
APP_ENV=development
LOG_LEVEL=info